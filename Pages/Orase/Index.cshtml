@page
@model Proiect.Pages.Orase.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Orase</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Oras[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oras[0].Descriere)
            </th>
            <th>
                @Html.LabelFor(model => model.Oras[0].Tara.Nume,"Tara")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Oras) {
        <tr>
                <td>
                @Html.DisplayFor(modelItem => item.Nume)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descriere)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tara.Nume)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
